# Core dependencies for VPinFE
requests
websockets
screeninfo
olefile
pynput
nicegui
platformdirs

# Self-contained Chromium window (replaces system Chrome dependency)
# PySide6 bundles WebEngine - no separate package needed
# LGPL licensed (vs PyQt6 which is GPL)
PySide6>=6.6.0
